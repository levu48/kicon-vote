# Prod: the live IdP. The `vote` client's prod redirect
# (https://vote.kicon.com/auth/callback) is registered there.
VITE_OIDC_AUTHORITY=https://auth.kicon.com

# The resource server.
VITE_API_BASE=https://api.kicon.com
